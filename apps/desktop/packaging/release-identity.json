{
  "//": "The frozen release constants (distribution spec §7.6, EDP-30). These are the values an installed VibeField is KNOWN BY: bundle id, publisher, tray slot, update origin. Changing one after a public release is a migration — signatures, update feeds, taskbar pinning and notification routing all key off them — never a cosmetic edit. Two laws are enforced by scripts/check-release-identity.mjs: (1) nothing here may be inferred from a local path, username or hostname (EDP-30) — a value that differs per developer machine is not an identity; (2) a `frozen` field cannot change without bumping identityRevision in the same commit, so an identity edit is always a reviewable event rather than a silent drift.",
  "identityRevision": 2,
  "frozenAt": null,
  "fields": {
    "appId": {
      "status": "frozen",
      "value": "com.jamesyong.vibefield",
      "format": "reverse-dns",
      "machineOverlapAcknowledged": "the namespace is deliberately the developer's own name, which is also this machine's username — a chosen personal namespace, not a leaked local value",
      "note": "Frozen 2026-07-25 (James). Personal namespace — the convention for an individually-enrolled Apple Developer account, and collision-free without owning a product domain. NOT the same thing as contracts' APP_ID ('vibefield'), which is the truffle mesh app id and data-root slug; see windowsAppUserModelId."
    },
    "productName": {
      "status": "frozen",
      "value": "VibeField",
      "format": "literal",
      "note": "Already load-bearing at runtime: app.setName('VibeField') in electron-shell's composition root, and the distribution spec §7.5 builder config."
    },
    "executableName": {
      "status": "frozen",
      "value": "VibeField",
      "format": "executable-name",
      "note": "Frozen 2026-07-25. Matches productName and electron-builder's own default, so Windows shortcuts, the Linux .desktop StartupWMClass and the macOS binary name all agree without a rename (distribution spec §6.3)."
    },
    "macTeamId": {
      "status": "blocker",
      "value": null,
      "format": "apple-team-id",
      "blocker": "B-3",
      "note": "Apple Developer Team ID — public information (it ships inside every signed app), unlike the signing certificate itself. A credential must NEVER be written here; the checker refuses anything shaped like one."
    },
    "windowsPublisher": {
      "status": "blocker",
      "value": null,
      "format": "publisher-name",
      "blocker": "B-2",
      "note": "The Authenticode/Trusted Signing subject shown in the UAC prompt. Must match the signing certificate's subject exactly or verification reports a publisher mismatch."
    },
    "windowsAppUserModelId": {
      "status": "frozen",
      "value": "com.jamesyong.vibefield",
      "format": "reverse-dns",
      "machineOverlapAcknowledged": "identical to appId by requirement; the same personal-namespace reasoning applies",
      "note": "Frozen 2026-07-25, deliberately IDENTICAL to appId: §5.6 requires the AUMID and the installer's application id to agree or taskbar pinning, shortcuts, notifications and updates disagree with each other. ACTION OUTSTANDING: electron-shell still calls app.setAppUserModelId(APP_ID) with contracts' mesh slug 'vibefield' — now that Windows is a GA target this is a real defect, and the runtime must switch to this value before the first Windows package."
    },
    "trayGuid": {
      "status": "frozen",
      "value": "c524c40e-05b6-4d89-bbb9-82ba4e97ea91",
      "format": "uuid",
      "note": "Minted 2026-07-25 once Windows became a GA target. Preserves the user's chosen notification-area slot across updates. CAUTION (§5.6): Windows keys the reserved slot to the SIGNED binary, so this GUID must not be used by an unsigned build — doing so burns it and the slot is lost for the signed one. It is therefore applied only from the first Authenticode-signed Windows package, not from a local --dir build."
    },
    "updateChannel": {
      "status": "frozen",
      "value": "beta",
      "format": "update-channel",
      "note": "First public channel. Stable clients never consume beta metadata (distribution spec §11.4); promotion to 'stable' is an identity event, hence the revision bump."
    },
    "updateOrigin": {
      "status": "blocker",
      "value": null,
      "format": "https-url",
      "blocker": "B-4",
      "note": "Where electron-updater looks for metadata. Public GitHub Releases vs authenticated object storage is undecided; either way no end-user machine may carry a token (distribution spec §11.4)."
    }
  }
}
