/**
 * The panels barrel. Settings owns user-facing canvas controls; engine
 * inspection and profiling live in ICE Devtools rather than duplicate app UI.
 */
export { SettingsPanel } from "./SettingsPanel";
export type { OverlapGlowConfig, OverlapGlowThemeColors, ThemeColors } from "./types";
