{
  "name": "vibe-field",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env tooling/dev-runner/src/cli.mjs",
    "dev:design": "node tooling/dev-runner/src/design-bench.mjs",
    "dev:design:web": "pnpm --filter @vibefield/electron-shell dev:design:web",
    "dev:onboarding": "node --env-file-if-exists=.env tooling/dev-runner/src/cli.mjs --force-onboarding",
    "smoke": "pnpm --filter @vibefield/desktop smoke",
    "smoke:canvas": "pnpm --filter @vibefield/desktop smoke:canvas",
    "playground": "node apps/plugin-playground/bin/plugin-playground.mjs",
    "plugin": "node tooling/plugin-cli/bin/vibefield-plugin.mjs",
    "smoke:godview": "pnpm --filter @vibefield/desktop smoke:godview",
    "lab:live-surfaces": "pnpm --filter @vibefield/desktop lab:live-surfaces",
    "bench:logging-baseline": "node scripts/bench-logging-baseline.mjs",
    "bench:logging-native": "cargo test --release -p field-native logging_reference_benchmark -- --ignored --nocapture",
    "build": "nx run @vibefield/desktop:build",
    "typecheck": "nx run-many -t typecheck",
    "test": "nx run-many -t test",
    "test:rust": "cargo test --workspace",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "format": "biome format --write . && cargo fmt",
    "icons:generate": "pnpm --filter @vibefield/desktop icons:generate",
    "icons:check": "pnpm --filter @vibefield/desktop icons:check",
    "gen": "pnpm --filter @vibefield/contracts gen:rust && pnpm gen:docs",
    "gen:docs": "node tooling/plugin-cli/bin/vibefield-plugin.mjs docs --out docs/plugin-authoring",
    "gen:manifests": "nx run-many -t gen:manifest",
    "gen:check": "pnpm gen && git diff --exit-code -- packages/contracts/gen packages/field-native/src/contracts.rs packages/field-native/src/registries.rs docs/plugin-authoring",
    "preflight": "node scripts/preflight.mjs",
    "bundle:assert": "pnpm --filter @vibefield/electron-shell run build:renderer && node scripts/bundle-report.mjs --dist packages/electron-shell/dist --assert",
    "verify": "pnpm preflight && pnpm icons:check && pnpm typecheck && pnpm lint && cargo fmt --check && pnpm lint:rust && pnpm bundle:assert && pnpm test && pnpm test:rust && pnpm gen:check",
    "hooks:install": "git config core.hooksPath scripts/git-hooks",
    "commit-draft": "sh scripts/commit-draft.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "nx": "23.1.0"
  },
  "license": "MIT"
}
