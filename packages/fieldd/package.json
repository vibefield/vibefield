{
  "name": "@vibefield/fieldd",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/mock-mgmt.ts"
  },
  "scripts": {
    "build": "esbuild src/bin.ts --bundle --platform=node --format=cjs --outfile=dist/bin.cjs --external:bufferutil --external:utf-8-validate",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vibefield/contracts": "workspace:*",
    "@vibefield/fieldd-client": "workspace:*",
    "@vibefield/plugin-build": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/ws": "^8.5.0",
    "esbuild": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
