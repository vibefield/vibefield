/**
 * The UI Bench gets no ambient host capabilities. Production components are
 * driven through deterministic renderer fixtures; a future Electron-specific
 * specimen must add an explicit, narrow bench contract instead of inheriting
 * the product preload and its user/daemon IPC surface.
 */
export {};
