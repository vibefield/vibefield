// GT-0 spike page entry (test-only; built under VITE_SPIKE=1). The page is a
// shell harness; the implementation lives behind field-app's /spike-godview
// export because only field-app carries the ghosttea-react dependency it
// exercises.
import "@vibefield/field-app/spike-godview";
