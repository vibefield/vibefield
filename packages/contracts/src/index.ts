// @vibefield/contracts — the single source of truth for every protocol VibeField owns.
// M1 (design-01 §11): schemas + registries + envelope + error taxonomy + fixture harness.
export const CONTRACTS_VERSION = "0.1.0";
